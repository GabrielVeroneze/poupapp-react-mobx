import styled from 'styled-components'

export const Usuario = styled.div`
    color: var(--cor-neutra-light);
    grid-area: usuario;

    & > h1 {
        margin: 16px 0 0 0;
    }

    & > p {
        margin: 8px 0 16px 0;
    }
`
